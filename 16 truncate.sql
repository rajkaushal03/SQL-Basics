-- Truncate ( Delete the table data );

TRUNCATE TABLE TABLE_name;

-- it delete all the data inside the table making all value equal to null 

--  the difference between drop and truncate is that drop delete the table whereas truncate only empty the table values or data;